Host-specific overrides
- Create files here, e.g. laptop.conf, desktop.conf, or host-myhost.conf
- Any .conf in this folder will be included at the end of kitty.conf
Examples:
  # includes/host/laptop.conf
  background_opacity 0.85

  # includes/host/desktop.conf
  background_opacity 0.95
